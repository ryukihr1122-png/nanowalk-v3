/**
 * NanoWalk — EvolutionAnimation v2.0（スタブ）
 *
 * 旧バトル設計の進化演出コンポーネント。
 * 新設計では進化はモンスター詳細画面のAlertで代替。
 * このコンポーネントは将来の演出強化用に保留。
 */

import { View } from "react-native";

// 現在未使用 — 将来の進化演出アニメーション用にファイルを保持
export function EvolutionAnimation(): null {
  return null;
}

export default EvolutionAnimation;
